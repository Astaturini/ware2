# Warehouse Simulator Version 0.1

Minimum working warehouse simulator with two robots, predefined tasks, obstacles,
a pickup station, and a dropoff station.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install flask
python app.py